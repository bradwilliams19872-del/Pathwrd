import { chatWithAiTutor } from "~/lib/ai-tutor";

// Re-export the server function so it's accessible as an API endpoint
// as well as via direct import from the shared lib.
export { chatWithAiTutor };
